/**
 * Stable diagnostic codes: the `code` field of every `--json` object.
 *
 * GENERATED by scripts/gen-diagnostic-codes.mjs -- do not edit by hand. Run the
 * script after adding a diagnostic; `npm test` fails while this file is stale.
 *
 * A code is a promise: NL2043 means the same rule next release as it does now.
 * `NL` is for Nish Lang -- the language is `Nish` everywhere a human reads it,
 * and `Nish Lang` is the longer form the project uses where a bare name would
 * be ambiguous, the way Rust writes `rust-lang`. The prefix is a literal here
 * rather than a value from `branding.ts`: a code is matched on by tooling that
 * has to keep working across a rename, so it is frozen by design.
 * The generator therefore only ever *appends* numbers, and a retired rule keeps
 * its number reserved. The bands follow the pipeline: NL0001 a syntax error,
 * NL1xxx Phase 0, NL2xxx the checker, NL3xxx the driver, NL4xxx the interop
 * sidecars, NL9xxx a WP15 section 8 performance warning, and NL0000 a
 * diagnostic no rule matched yet.
 *
 * Codes are carried in `--json` only. The human summary line
 * `file:line:col: error: <text>` is unchanged and stays byte-for-byte what it
 * has always been, because the `.err` goldens and `tests/self/reject_oracle.js`
 * match on it.
 *
 * A fragment is the longest literal run of a message's template -- the rule in
 * words, with the interpolated names, types and counts removed -- and is matched
 * as a substring, because many messages open with the thing they are about and
 * name the broken rule only afterwards. Cutting at the interpolations is also
 * why no fragment here spells the project's name: "... is forbidden in
 * ${LANGUAGE}" contributes the run before the name, leaving `branding.ts` the
 * only place it lives.
 */

/** No rule matched: the diagnostic has no code yet. */
export const UNCODED: string = "NL0000";

/** Every syntax error shares one code: stage0 takes that text from the `typescript` package. */
export const SYNTAX: string = "NL0001";

/**
 * Band 0 is what is wrong with the *run* rather than with the program: the C
 * toolchain `--link` needs could not be used (exit 3), and an internal compiler
 * error (exit 70). Neither has a source location.
 */
export const TOOLCHAIN: string = "NL0002";
export const INTERNAL: string = "NL0003";

/** Number of rules that carry a code; `tests/run.js` checks it against stage0's. */
export const RULE_COUNT: i32 = 399;

/**
 * Fragment, code, fragment, code -- flat because the language has no tuple, and
 * a function rather than a module constant because a constant's initialiser
 * must be a literal (`self/target.ts` holds its table the same way). The
 * function is an arrow bound to a `const`, which is how `self/` declares one
 * since WP22 stage C; a concise body is exactly what a table-returning function
 * wants. Longest fragment first, so a specific rule wins over a general one it
 * contains.
 */
export const diagnosticRules = (): string[] => [
  "` (supported: number, i32, i64, u8, u16, u32, u64, f32, f64, boolean, string, void, T[], Result<T, E>, Int32Array/Float64Array/BigInt64Array, and declared classes/interfaces)",
  "NL2039",
  " (it aborts rather than unwinding): return a `Result<T, E>` for a failure a caller should handle, or `panic(message)` to end the process",
  "NL1001",
  "(...)` needs a contextual `Result<T, E>` type (annotate the function's return type, e.g. `function f(): Result<number, string>`)",
  "NL2035",
  " | null` is not supported: only class, interface, array, and string types can be nullable (a scalar has no null value)",
  "NL2010",
  "`case` label must be an integer literal, a module constant, or an enum member (LLVM's `switch` table holds constants)",
  "NL2284",
  "Type aliases cannot be exported: an alias names a type inside one module (declare it in every module that needs it)",
  "NL2276",
  "`const enum` is not supported: an enum member is already folded to its integer, so `const` would ask for nothing",
  "NL2285",
  "; a class or interface name must be unique across the program, and an instantiation is named after its template",
  "NL3013",
  "`main` cannot be generic: the entry point is called by the C runtime, which has no type arguments to give it",
  "NL2300",
  "`super(...)` is only valid as the first statement of the constructor of a class that `extends` another class",
  "NL2155",
  "` exceeds 2^53 and cannot be written exactly (the parser already rounded it); compute the i64 value instead",
  "NL2055",
  "Enums cannot be exported: an enum names a type inside one module (declare it in every module that needs it)",
  "NL2288",
  "Object literal needs a contextual class or interface type (annotate the variable: `const p: P = { ... }`)",
  "NL2224",
  "Parameter properties (`constructor(public x: number)`) are not supported; declare the field and assign it",
  "NL2234",
  "`extends` is not supported: Nish has no inheritance. Declare the base's fields as the first fields of `",
  "NL2278",
  "A `case` clause cannot declare a variable directly; wrap the clause body in a block (`case 1: { ... }`)",
  "NL2165",
  "; a function name must be unique across the program, and an instantiation is named after its template",
  "NL3010",
  "`export { ... }` / `export * from` are not supported; put `export` on the function declaration itself",
  "NL2128",
  "`case` label must be an integer literal or a module constant (LLVM's `switch` table holds constants)",
  "NL2123",
  "; if (r.isErr()) { ... }`), propagate it with `.orReturn()`, or end on it with `.expect(message)`",
  "NL2025",
  "`super` can only be used as `super.method(...)`, or as `super(...)` at the start of a constructor",
  "NL2157",
  "`, and the push may move that storage first; copy the fields you need into locals before pushing",
  "NL2291",
  "`null` needs a contextual `T | null` type (annotate the variable, e.g. `let p: P | null = null`)",
  "NL2145",
  "`declare function` cannot be generic: a C symbol is one function, not a template to instantiate",
  "NL2307",
  "`Result<T, E> | null` is not supported: a `Result` already models absence through its error arm",
  "NL2151",
  "A `case` clause with statements must end in `break`, `return`, `continue` or `process.exit` (",
  "NL2166",
  "`super` is only valid inside a method or constructor of a class that `extends` another class",
  "NL2159",
  "A module constant's initialiser must be a literal, another constant, or arithmetic over them",
  "NL2167",
  "` allocates a dynamically sized array on every iteration of this loop and nothing keeps it ",
  "NL2042",
  "; a class or interface name is still program-wide, so two packages cannot both declare one",
  "NL3009",
  "` exceeds 2^53 and cannot be written exactly (the parser already rounded it); compute the ",
  "NL2054",
  "` is rebuilt from its own value on every iteration of this loop, so every pass copies all ",
  "NL2087",
  "` (Phase 1 supports number, i32, i64, u8, u16, u32, u64, f32, f64, boolean, string, void)",
  "NL2038",
  "`process.argv` requires a `main` entry point (this program has no `export function main`)",
  "NL2149",
  "`Result<T, void>` is not supported: an error must carry a value (use `Result<T, string>`)",
  "NL2152",
  "`super` is not supported: Nish has no inheritance, so a class has no base class to reach",
  "NL2279",
  "` is not a module constant, and a module constant's value must be known at compile time",
  "NL2080",
  "Only functions can be exported for now, plus classes and interfaces (found `export` on ",
  "NL2226",
  "` under a type constructor instead of passing it on, so the chain has no end; name `",
  "NL2319",
  "` under a type constructor instead of passing it on, so the chain has no end; pass `",
  "NL2309",
  "`declare function` declares a C function this program calls, so it must have no body",
  "NL2308",
  ": a `Result` is immutable once built (return a new `ok(...)` or `err(...)` instead)",
  "NL2028",
  ": a `Result` is immutable once built (return a new `Ok(...)` or `Err(...)` instead)",
  "NL2270",
  "`; check the nullable side against `null` first, then compare the narrowed values",
  "NL2114",
  ": --emit-napi-async requires --threads (its exports allocate on a worker thread)",
  "NL3012",
  "` is not supported; a module has no top-level code, so only `const` is available",
  "NL2084",
  "Operator `+` requires two operands of the same numeric type or two strings, got ",
  "NL2231",
  "` cannot be exported: it is a C function this program calls, not one it defines",
  "NL2306",
  "`export default` / `export =` are not supported; use a named `export function`",
  "NL2129",
  "`ReadonlyArray` needs exactly one type argument, e.g. `ReadonlyArray<number>`",
  "NL2266",
  "`main` cannot take parameters (command-line arguments are not supported yet)",
  "NL2139",
  "`for...of` needs a `const` or `let` declaration, e.g. `for (const x of xs)`",
  "NL2135",
  "attempt to compute with overflow in a constant: the result does not fit in ",
  "NL2175",
  "Empty array literal needs a type annotation, e.g. `const xs: number[] = []`",
  "NL2199",
  ": `static` members are not supported (use a top-level function or const)",
  "NL2027",
  ": initializers must be literals (assign other values in the constructor)",
  "NL2030",
  "`new Array` needs exactly one type argument, e.g. `new Array<number>(n)`",
  "NL2142",
  "`Result` needs exactly two type arguments, e.g. `Result<number, string>`",
  "NL2150",
  "` values; compare each with `null` and then compare the narrowed values",
  "NL2108",
  "`main` must return void or an i32 number (the process exit code), not ",
  "NL2140",
  "Only relative import specifiers are supported (`./x` or `../x`), got `",
  "NL2227",
  ".isErr()`, or use `.orReturn()`, `.unwrapOr(v)` or `.expect(message)`",
  "NL2032",
  "Only top-level function declarations are supported in Phase 1 (found ",
  "NL2230",
  "The `for...of` variable takes the element type; remove the annotation",
  "NL2242",
  " (the interface's fields must be the class's first fields, in order)",
  "NL2277",
  "Interface inheritance (`extends`) is not supported; list every field",
  "NL2215",
  " (use `if` / `else`; only an integer switch lowers to a jump table)",
  "NL2009",
  "` expects a number (i32, i64, u8, u16, u32, u64, f32, or f64), got ",
  "NL2057",
  "` is not supported: inherited fields are read and written as `this.",
  "NL2085",
  "Function names starting with `nish_` are reserved for the runtime",
  "NL2202",
  "; convert the error first: `if (r.isErr()) { return Err(...); }`",
  "NL2023",
  "`; a derived class cannot redeclare or shadow an inherited field",
  "NL2112",
  "`: `static` members are not supported (use a top-level function)",
  "NL2116",
  "`export default` is not supported; use a named `export function`",
  "NL2130",
  "`super(...)` must be the first statement of the constructor of `",
  "NL2156",
  ", not in your program. Please report it with the input file and",
  "NL3002",
  "` is generic: it must be written with its type arguments, e.g. ",
  "NL2317",
  "`new Array<T>(n)` expects exactly 1 argument (the length), got ",
  "NL2143",
  " (it has `isOk`, `isErr`, `orReturn`, `unwrapOr` and `expect`)",
  "NL2005",
  "` takes its signature from the arrow; drop the annotation on `",
  "NL2273",
  "Template literal hole must be string, number, or boolean, got ",
  "NL2238",
  "` is a built-in type name and cannot be used for a type alias",
  "NL2275",
  "`Array` needs exactly one type argument, e.g. `Array<number>`",
  "NL2121",
  "`extends` must name exactly one class declared in this module",
  "NL2132",
  "` again afterwards rather than holding the element across it",
  "NL2290",
  "` cannot be used before `super(...)` in the constructor of `",
  "NL2047",
  "` expects 0 argument(s) (the class has no constructor), got ",
  "NL2056",
  "` in a constant requires two operands of the same type, got ",
  "NL2070",
  "`unwrapOr(...)` needs a success value to fall back to, and `",
  "NL2164",
  " (an override keeps the signature; there is no overloading)",
  "NL2001",
  "` cannot declare methods (interfaces describe layout only)",
  "NL2048",
  "the command line at https://github.com/amritk/nish/issues",
  "NL3007",
  " generic instantiations, which is this compiler's limit ",
  "NL2295",
  ": definite-assignment assertions (`!`) are not supported",
  "NL2029",
  "` is a built-in type name and cannot be used for an enum",
  "NL2281",
  "`: a type parameter is inferred from the arguments, and ",
  "NL2299",
  "`this` cannot be used as a value in the constructor of `",
  "NL2161",
  "Array literal elements must all have the same type, got ",
  "NL2172",
  "Names starting with `nish_` are reserved for the runtime",
  "NL2220",
  "Only the entry module may declare `export function main`",
  "NL2229",
  " = ...`) is not supported: a type argument is inferred ",
  "NL2292",
  "` is a generic function, and a generic function cannot ",
  "NL2298",
  "` is read before it is assigned in the constructor of `",
  "NL2086",
  "`export default` is not supported; use a named `export`",
  "NL2131",
  " extends ...`) is not supported yet: an unconstrained ",
  "NL2294",
  " produces no value and can only be used as a statement",
  "NL2020",
  "` here: it only holds an error where the call failed; ",
  "NL2067",
  "` requires two operands of the same integer type, got ",
  "NL2101",
  "` requires two operands of the same numeric type, got ",
  "NL2102",
  "\"`) are not supported; modules have no top-level code",
  "NL2033",
  "Generic type parameters are forbidden on a method in ",
  "NL1055",
  "` must start with `super(...)`: the constructor of `",
  "NL2094",
  "; there is no top-level code to build anything else",
  "NL2026",
  "); handle it here instead: `if (r.isErr()) { ... }`",
  "NL2037",
  "` (the base's constructor and methods are part of `",
  "NL2040",
  "` in a constant of integer type (annotate it `f64`)",
  "NL2068",
  "`Number` expects a string, number, or boolean, got ",
  "NL2146",
  "`readonly` is only permitted on an array type, got ",
  "NL2265",
  "`this` is only valid inside a method or constructor",
  "NL2162",
  "Union types other than `T | null` are forbidden in ",
  "NL1053",
  " cannot be optional (every field has a fixed slot)",
  "NL2015",
  "` has no initializer and no constructor assigns it",
  "NL2066",
  "` returns void and can only be used as a statement",
  "NL2105",
  "`; check for null first: `if (a !== null) { ... }`",
  "NL2113",
  "Only direct calls to named functions are supported",
  "NL2225",
  "The `for...of` variable cannot have an initializer",
  "NL2241",
  " modules listed below. Link the .ll module(s) and",
  "NL4002",
  ", and a declared C function returns a scalar only",
  "NL2304",
  "` has no `value`: its success arm carries nothing",
  "NL2064",
  "`namespace` and `module` blocks are forbidden in ",
  "NL1027",
  "Default imports are not supported; use `import { ",
  "NL2190",
  "Index signatures are not supported in interface `",
  "NL2214",
  "Type arguments are not written at a call site in ",
  "NL2303",
  "Getters and setters are not supported in class `",
  "NL2209",
  " (build the parts with template literals first)",
  "NL2003",
  "` is not definitely assigned in the constructor",
  "NL2082",
  "` is not part of the standard library (it has: ",
  "NL3011",
  " values; build it with `[]` and `push` instead",
  "NL2022",
  ", and a declared C function takes scalars only",
  "NL2305",
  "` has more than one constructor (no overloads)",
  "NL2063",
  "` is a builtin function and can only be called",
  "NL2312",
  "` requires two operands of the same type, got ",
  "NL2103",
  "`) are not supported; import functions by name",
  "NL2119",
  "Operator `~` requires an integer operand, got ",
  "NL2232",
  "Ternary branches must have the same type, got ",
  "NL2240",
  "; use `obj.name` (no dynamic property lookup)",
  "NL1009",
  "` is already declared as a class or interface",
  "NL2074",
  "` takes no type argument (it is an alias of `",
  "NL2107",
  "attempt to divide with overflow in a constant",
  "NL2177",
  "Index signatures are not supported in class `",
  "NL2213",
  "Object literal keys must be plain identifiers",
  "NL2223",
  "Optional/default parameters are not supported",
  "NL2233",
  "Regular expression literals are forbidden in ",
  "NL1050",
  "` in i32 number mode (use --number-mode f64)",
  "NL2071",
  "Type assertion to `unknown` is forbidden in ",
  "NL1052",
  " functions (identical to --emit-header). */",
  "NL4001",
  "` must have a body (no overload signatures)",
  "NL2091",
  "` takes no type argument (it is `new Array<",
  "NL2106",
  "`implements` must name a declared interface",
  "NL2137",
  "`switch` has more than one `default` clause",
  "NL2160",
  "Element access requires a numeric index in ",
  "NL1042",
  "Generic type parameters are forbidden on a ",
  "NL1054",
  ": `$` separates a generic's name from its ",
  "NL2296",
  "` accepts string, number, or boolean, got ",
  "NL2041",
  "` needs an explicit return type annotation",
  "NL2096",
  "`substring` expects 1 or 2 arguments, got ",
  "NL2153",
  "Loose equality is forbidden; use === / !==",
  "NL1047",
  " (no runtime metadata or class rewriting)",
  "NL1006",
  " (object layout is fixed at compile time)",
  "NL1007",
  "` in a constant requires boolean operands",
  "NL2069",
  "` is a builtin value and cannot be called",
  "NL2314",
  "An enum member name must be an identifier",
  "NL2287",
  "Computed property names are forbidden in ",
  "NL1041",
  "Constructors cannot declare a return type",
  "NL2189",
  "Destructured parameters are not supported",
  "NL2192",
  "Enum members must be numeric literals in ",
  "NL1043",
  "Generic type parameters are forbidden in ",
  "NL1045",
  "Holes in array literals are not supported",
  "NL2210",
  "Import must name the functions it imports",
  "NL2211",
  "Import specifier must be a string literal",
  "NL2212",
  "Spread in array literals is not supported",
  "NL2237",
  "Unary `+` is forbidden; it converts, and ",
  "NL2271",
  " (array length is read-only; use `push`)",
  "NL2002",
  "`for...of` declares exactly one variable",
  "NL2134",
  "`try`/`catch`/`finally` is forbidden in ",
  "NL1033",
  "`var` is forbidden; use `let` or `const`",
  "NL1036",
  "Destructured constants are not supported",
  "NL2191",
  "Nullish coalescing `??` is forbidden in ",
  "NL1048",
  "Type assertion to `any` is forbidden in ",
  "NL1051",
  " (modules are resolved at compile time)",
  "NL1002",
  "`declare function` is not supported yet",
  "NL2125",
  "`Function` constructor is forbidden in ",
  "NL1021",
  "attempt to divide by zero in a constant",
  "NL2176",
  "Optional chaining `?.` is forbidden in ",
  "NL1049",
  "Return type mismatch: function returns ",
  "NL2236",
  " (fields must match exactly, in order)",
  "NL2004",
  "`slice` expects 1 or 2 arguments, got ",
  "NL2289",
  "Cannot declare a variable of type void",
  "NL2183",
  "` must be declared `const`, not `let`",
  "NL2272",
  "`break` outside of a loop or `switch`",
  "NL2122",
  "`join` expects 0 or 1 arguments, got ",
  "NL2138",
  "A function declaration binds one name",
  "NL2274",
  "Only simple variables can be assigned",
  "NL2228",
  " (use --number-mode f64 or toF64(x))",
  "NL2008",
  "` is already declared in this module",
  "NL2076",
  "` is set twice in the object literal",
  "NL2088",
  "`.prototype` access is forbidden in ",
  "NL1012",
  "`): the type name is part of the ABI",
  "NL2120",
  "`super(...)` in the constructor of `",
  "NL2154",
  "`void` expressions are forbidden in ",
  "NL1037",
  "Labeled statements are forbidden in ",
  "NL1046",
  "Labelled `continue` is not supported",
  "NL2218",
  " (it has `ok`, `value` and `error`)",
  "NL2006",
  " (no dynamic property interception)",
  "NL1003",
  "; use `null` with a `T | null` type",
  "NL1008",
  "` is already declared as a function",
  "NL2075",
  "` requires a numeric variable, got ",
  "NL2098",
  "`__proto__` access is forbidden in ",
  "NL1010",
  "`; use an object literal: `{ ... }`",
  "NL2115",
  "`async` functions are forbidden in ",
  "NL1015",
  "`bigint` literals are forbidden in ",
  "NL1016",
  "Array length must be a number, got ",
  "NL2171",
  "Comma expressions are forbidden in ",
  "NL1040",
  "Generic functions are not supported",
  "NL2207",
  "internal: array method on non-array",
  "NL2216",
  "Type-only imports are not supported",
  "NL2243",
  "Unsupported expression in Phase 1: ",
  "NL2256",
  "Unsupported object literal member: ",
  "NL2258",
  "` expects exactly 1 argument, got ",
  "NL2061",
  "` must declare at least one member",
  "NL2283",
  "` must have plain identifier names",
  "NL2092",
  "`expect(...)` expects string, got ",
  "NL2127",
  "`for...of` requires an array, got ",
  "NL2136",
  "Abstract classes are not supported",
  "NL2168",
  "Array index must be a number, got ",
  "NL2170",
  "Unsupported statement in Phase 1: ",
  "NL2260",
  "Unsupported unary operator `-` on ",
  "NL2262",
  "Unsupported unary operator `!` on ",
  "NL2263",
  "` because it is a module constant",
  "NL2043",
  "` but not exported (add `export`)",
  "NL2045",
  "` requires boolean operands, got ",
  "NL2099",
  "`declare global` is forbidden in ",
  "NL1019",
  "`Math.abs` expects a number, got ",
  "NL2141",
  "async functions are not supported",
  "NL2173",
  "Cannot assign to readonly field `",
  "NL2181",
  "Generic classes are not supported",
  "NL2206",
  "Generic methods are not supported",
  "NL2208",
  "Labelled `break` is not supported",
  "NL2217",
  "Rest parameters are not supported",
  "NL2235",
  "Unsupported assignment operator `",
  "NL2253",
  "Unsupported interface member in `",
  "NL2257",
  "(...)` is a `Result<T, E>`, not ",
  "NL2034",
  "`, which does not extend a class",
  "NL2111",
  "`Function` type is forbidden in ",
  "NL1023",
  "`super` expects 0 argument(s) (`",
  "NL2158",
  "Expected a return value of type ",
  "NL2201",
  "` before the error is handled; ",
  "NL2044",
  "` is a constant imported from `",
  "NL2072",
  "` is a function imported from `",
  "NL2073",
  "` is defined in terms of itself",
  "NL2077",
  "` when checking a single module",
  "NL2109",
  "`declare enum` is not supported",
  "NL2286",
  "`new Function` is forbidden in ",
  "NL1028",
  "async methods are not supported",
  "NL2174",
  "Cannot assign to an element of ",
  "NL2267",
  "Condition must be boolean, got ",
  "NL2188",
  "Ternary branches cannot be void",
  "NL2239",
  "--target: unsupported target `",
  "NL3001",
  "` expects a number index, got ",
  "NL2058",
  "` expects an argument of type ",
  "NL2268",
  "` is a builtin imported from `",
  "NL2313",
  "` must return a value of type ",
  "NL2093",
  "`bigint` type is forbidden in ",
  "NL1017",
  "`in` operator is forbidden in ",
  "NL1025",
  "`symbol` type is forbidden in ",
  "NL1031",
  "`Symbol` type is forbidden in ",
  "NL1032",
  "Destructuring is not supported",
  "NL2193",
  "` is not a declared interface",
  "NL2079",
  "`globalThis` is forbidden in ",
  "NL1024",
  "`instanceof` is forbidden in ",
  "NL1026",
  "Array elements cannot be void",
  "NL2169",
  "Cannot index a value of type ",
  "NL2184",
  "Unsupported binary operator `",
  "NL2254",
  "Unsupported class member in `",
  "NL2255",
  " (no event loop or promises)",
  "NL1004",
  " (no interpreter at runtime)",
  "NL1005",
  "` is already imported from `",
  "NL2315",
  "` must be an integer literal",
  "NL2282",
  "`__proto__` is forbidden in ",
  "NL1011",
  "`arguments` is forbidden in ",
  "NL1014",
  "`continue` outside of a loop",
  "NL2124",
  "`for await` is not supported",
  "NL2133",
  "Generators are forbidden in ",
  "NL1044",
  "Generators are not supported",
  "NL2205",
  "Unsupported unary operator `",
  "NL2261",
  "()` in the constructor of `",
  "NL2036",
  "` (the builtin modules are ",
  "NL2310",
  "` cannot extend interface `",
  "NL2049",
  "`debugger` is forbidden in ",
  "NL1018",
  "`Function` is forbidden in ",
  "NL1022",
  "`new` requires a class name",
  "NL2144",
  "`process.argv` is read-only",
  "NL2148",
  "Cannot assign to property `",
  "NL2180",
  ": cannot create the result",
  "NL4003",
  "` expects a value of type ",
  "NL2269",
  "` is not generic, so `new ",
  "NL2318",
  "`, which already extends `",
  "NL2110",
  "`Reflect` is forbidden in ",
  "NL1029",
  "`unknown` is forbidden in ",
  "NL1035",
  "Duplicate declaration of `",
  "NL2195",
  "Duplicate type parameter `",
  "NL2302",
  "Functions must have a body",
  "NL2204",
  " but the discriminant is ",
  "NL2014",
  ": internal compiler error",
  "NL3003",
  "` needs a type annotation",
  "NL2095",
  "`: unsupported modifier `",
  "NL2118",
  "`delete` is forbidden in ",
  "NL1020",
  "`Symbol` is forbidden in ",
  "NL1030",
  "`typeof` is forbidden in ",
  "NL1034",
  " but its initializer is ",
  "NL2013",
  " needs a type annotation",
  "NL2019",
  ": unsupported modifier `",
  "NL2031",
  "` expects a string, got ",
  "NL2059",
  "` has been instantiated ",
  "NL2297",
  "` returns before field `",
  "NL2104",
  "`unwrapOr(...)` expects ",
  "NL2163",
  "`yield` is forbidden in ",
  "NL1039",
  "Duplicate `case` label `",
  "NL2194",
  "; declare the type as `",
  "NL2024",
  "`with` is forbidden in ",
  "NL1038",
  "=1 for the stack trace)",
  "NL3005",
  "Functions must be named",
  "NL2203",
  "Unreachable code after ",
  "NL2251",
  " (the element type of ",
  "NL2007",
  " are not supported yet",
  "NL2293",
  " cannot have type void",
  "NL2016",
  " clashes with method `",
  "NL2017",
  "` cannot extend itself",
  "NL2050",
  "` clashes with field `",
  "NL2051",
  "` is not available on ",
  "NL2081",
  "` needs an initialiser",
  "NL2097",
  "`any` is forbidden in ",
  "NL1013",
  "Cannot read property `",
  "NL2187",
  "could not be converted",
  "NL4005",
  "Unsupported operator `",
  "NL2259",
  " to write through it)",
  "NL2264",
  "` does not fit in i32",
  "NL2053",
  "` must be initialized",
  "NL2089",
  "Duplicate parameter `",
  "NL2198",
  "Non-integer literal `",
  "NL2222",
  "` cannot be optional",
  "NL2046",
  "` is missing field `",
  "NL2078",
  "Cannot find module `",
  "NL3006",
  "Duplicate function `",
  "NL2196",
  "Unknown identifier `",
  "NL2248",
  "` expects f64, got ",
  "NL2062",
  "`, which needs the ",
  "NL4004",
  "`Err(...)` expects ",
  "NL2126",
  "Negative literal `-",
  "NL2221",
  " are not supported",
  "NL2011",
  " argument(s), got ",
  "NL2012",
  " to an element of ",
  "NL2021",
  "` does not fit in ",
  "NL2052",
  "` expects exactly ",
  "NL2060",
  "` is not supported",
  "NL2083",
  "` must have a body",
  "NL2090",
  "`Ok(...)` expects ",
  "NL2147",
  "Cannot assign to `",
  "NL2179",
  "Cannot initialize ",
  "NL2185",
  "Duplicate member `",
  "NL2197",
  "Unknown function `",
  "NL2247",
  "Unknown property `",
  "NL2250",
  "` has no export `",
  "NL2311",
  "` has no member `",
  "NL2280",
  "Empty import list",
  "NL2200",
  "Module constant `",
  "NL2219",
  "Unknown builtin `",
  "NL2244",
  "Unsupported `new ",
  "NL2252",
  "` has no field `",
  "NL2065",
  "must be a string",
  "NL4006",
  "Unknown method `",
  "NL2249",
  "unknown option: ",
  "NL3008",
  "` is a generic ",
  "NL2316",
  "` requires two ",
  "NL2100",
  "Unknown class `",
  "NL2245",
  "Unknown field `",
  "NL2246",
  " must be named",
  "NL2018",
  "Cannot assign ",
  "NL2178",
  "Cannot infer `",
  "NL2301",
  "Cannot call `",
  "NL2182",
  "`: expected ",
  "NL2117",
  "Cannot push ",
  "NL2186",
];

/** The WP15 section 8 rules, matched by substring: their message opens with a variable name. */
export const performanceRules = (): string[] => [
  "allocates a dynamically sized array on every iteration of this loop",
  "NL9001",
  "is rebuilt from its own value on every iteration of this loop",
  "NL9002",
  "already holds an allocation and this one drops it",
  "NL9003",
  "this computes with overflow: the result",
  "NL9006",
  "is computed in i32 and wraps before",
  "NL9005",
  "is not proven to be in range for",
  "NL9007",
  "is at or beyond the",
  "NL9004",
];

/**
 * The code for one diagnostic. `kind` is the word in the summary line
 * (`error`, `syntax error`, `performance`) and `text` the message without
 * its location prefix.
 */
export const codeFor = (kind: string, text: string): string => {
  if (kind === "syntax error") {
    return SYNTAX;
  }
  if (kind === "performance") {
    const perf: string[] = performanceRules();
    let i: i32 = 0;
    while (i < perf.length) {
      if (text.indexOf(perf[i]) >= 0) {
        return perf[i + 1];
      }
      i = i + 2;
    }
    return UNCODED;
  }
  const rules: string[] = diagnosticRules();
  let j: i32 = 0;
  while (j < rules.length) {
    if (text.indexOf(rules[j]) >= 0) {
      return rules[j + 1];
    }
    j = j + 2;
  }
  return UNCODED;
};

// `integer` is reserved for ranged integers (WP31): a class of that name would
// make `integer<0, 255>` mean two things in this module.
class integer {
  value: i32 = 0;
}

export const test = (): number => 0;

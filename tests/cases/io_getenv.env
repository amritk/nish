# `getenv` answers exactly these. The last line unsets rather than omits:
# a developer with AMRITC_TEST_NOT_SET exported would otherwise fail the golden.
AMRITC_TEST_VALUE=clang-18
AMRITC_TEST_EMPTY=
AMRITC_TEST_NOT_SET

# `getenv` answers exactly these. The last line unsets rather than omits:
# a developer with NISH_TEST_NOT_SET exported would otherwise fail the golden.
NISH_TEST_VALUE=clang-18
NISH_TEST_EMPTY=
NISH_TEST_NOT_SET

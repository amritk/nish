// WP27 S1: the return position has the same rule as a parameter, and a
// returned pointer is the harder half — its lifetime is unknown to the arena.
declare function m(): string;

export function main(): i32 {
  return 0;
}

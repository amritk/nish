// WP21 S2 resolves a package name, so the refusal moved: a bare specifier is
// legal and this one simply is not installed. The name is under a scope that
// cannot exist, so the case says the same thing in any checkout.
import { chunk } from "@nish-absent/lodash";

export const main = (): number => 0;

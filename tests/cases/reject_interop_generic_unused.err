:6:14: error: `identity` is generic, so it has no single C signature: a generic function becomes a symbol only where it is instantiated, and this program instantiates none
:8:14: error: `Box` is generic, so it has no single C layout: a generic class becomes a struct only where it is instantiated, and this program instantiates none

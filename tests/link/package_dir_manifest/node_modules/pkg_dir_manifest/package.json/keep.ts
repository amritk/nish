// `package.json` is a *directory* here, which is the second thing a tree can
// point either compiler's walk at that opens and is not a file. This file only
// exists because a directory with nothing in it is not a thing git stores.
export const unused = (): i32 => 0;

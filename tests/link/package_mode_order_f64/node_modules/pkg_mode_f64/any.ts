// The mode-agnostic entry, declared above `nish-f64` and so the file a
// declaration-order match would compile for an f64 program. The other half of
// the pair is `tests/link/package_mode_order`, which asks the same question of
// `nish-i32`; this one is the mode the demonstration in
// `docs/wp21-packages.md` §6 is about, where `/` means something else.
export const seed = (): i32 => 1;

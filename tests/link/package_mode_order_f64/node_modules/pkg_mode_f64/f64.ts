// Selected by the `nish-f64` condition this program's `--number-mode f64`
// asks for, though the manifest declares it after plain `nish`. `helper` is
// private to the package and carries its prefix (WP21 S1).
const helper = (): i32 => 50;

export const seed = (): i32 => helper() + helper();

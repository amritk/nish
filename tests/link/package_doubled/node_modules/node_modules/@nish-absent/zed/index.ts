// The `.` entry of `zed`, which is installed one `node_modules` inside another.
// `helper` is private to the package and shares its name with the program's.
const helper = (): i32 => 5;

export const zed = (): i32 => helper();

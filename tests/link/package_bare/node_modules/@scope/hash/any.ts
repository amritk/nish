// The mode-agnostic entry, selected when `nish-i32` is not the mode asked for.
// A different answer on purpose: the exit code is what says which file the
// condition picked.
export const seed = (): i32 => 1;

// The one real `shared`. The program reaches it by its own name and `app2`
// reaches it through a symlink, so the two paths name one file — and this
// compiler does not know that (see `main.ts`).
export const val = (): i32 => 3;

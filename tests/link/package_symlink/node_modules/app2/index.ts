// `app2` depends on `shared` and reaches it through
// `node_modules/app2/node_modules/shared`, which is a symlink to the copy the
// program itself imports. Node resolves both to one module; this compiler
// resolves two.
import { val } from "shared";

export const twice = (): i32 => val() * 2;

// The file the package meant, one directory below the target its `exports`
// names. Nothing reaches it: `./src` is where the resolver is sent, and `./src`
// is a directory.
export const seed = (): i32 => 4;

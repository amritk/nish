// The other half: the same name, a different layout. Silently merging the two
// would let a `Node` from one package pass for a `Node` from the other.

class Node {
  x: i32 = 2;
  y: i32 = 3;
}

export const second = (): i32 => new Node().y;

// The mode-agnostic entry, and the one this program must *not* get: the
// manifest declares it above `nish-i32`, so a resolver that took the first
// condition the manifest names would compile this file and leave `./i32.ts` —
// the source the package wrote for this very mode — dead, with nothing said.
export const seed = (): i32 => 1;

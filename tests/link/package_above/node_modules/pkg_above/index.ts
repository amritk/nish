// The `.` entry of `pkg_above`, named by the `nish` condition of its `exports`.
// `helper` is private to the package and shares its name with the program's.
const helper = (): i32 => 4;

export const scale = (n: i32): i32 => n * helper();

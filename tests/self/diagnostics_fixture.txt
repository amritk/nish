export function add(a: number, b: number): number {
	return a + b;
}

export function main(): number {
  const total = add(1, 2);
  console.log(`total ${total}`);
  return total;
}

// a deliberately long line so the caret has somewhere to run: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

export function last(): number { return 0; }

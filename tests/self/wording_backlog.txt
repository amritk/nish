# The diagnostic wordings that no surviving case exercises (WP19 G2.4).
#
# Every code here is a message the compiler can print and nothing checks the
# words of. Today they are proved by stage0 and stage1 agreeing; after R6 that
# comparison is gone and they would be proved by nothing, so this file is a
# work list rather than a list of exemptions.
#
# GENERATED by scripts/check-diagnostic-coverage.mjs --update. The check is a
# ratchet: a code that leaves this file may not come back, and a code that is
# not here and not covered fails the run. Write a `reject_*` case, run the
# script with --update, and the line disappears.
#
# 56 live codes, measured against tests/cases/reject_* and
# tests/link/. Retired fragments are not here and never will be: they match no
# message, so no case can reach them.

NL1021  # `Function` constructor is forbidden in
NL1032  # `Symbol` type is forbidden in
NL1039  # `yield` is forbidden in
NL2011  #  are not supported
NL2019  #  needs a type annotation
NL2031  # : unsupported modifier `
NL2038  # ` (Phase 1 supports number, i32, i64, u8, u16, u32, u64, f32, f64, boolean, string, void)
NL2054  # ` exceeds 2^53 and cannot be written exactly (the parser already rounded it); compute the
NL2055  # ` exceeds 2^53 and cannot be written exactly (the parser already rounded it); compute the i64 value instead
NL2062  # ` expects f64, got
NL2068  # ` in a constant of integer type (annotate it `f64`)
NL2069  # ` in a constant requires boolean operands
NL2072  # ` is a constant imported from `
NL2073  # ` is a function imported from `
NL2075  # ` is already declared as a function
NL2083  # ` is not supported
NL2098  # ` requires a numeric variable, got
NL2109  # ` when checking a single module
NL2124  # `continue` outside of a loop
NL2125  # `declare function` is not supported yet
NL2133  # `for await` is not supported
NL2173  # async functions are not supported
NL2174  # async methods are not supported
NL2177  # attempt to divide with overflow in a constant
NL2178  # Cannot assign
NL2182  # Cannot call `
NL2183  # Cannot declare a variable of type void
NL2192  # Destructured parameters are not supported
NL2193  # Destructuring is not supported
NL2195  # Duplicate declaration of `
NL2201  # Expected a return value of type
NL2203  # Functions must be named
NL2205  # Generators are not supported
NL2207  # Generic functions are not supported
NL2208  # Generic methods are not supported
NL2211  # Import must name the functions it imports
NL2212  # Import specifier must be a string literal
NL2215  # Interface inheritance (`extends`) is not supported; list every field
NL2216  # internal: array method on non-array
NL2217  # Labelled `break` is not supported
NL2218  # Labelled `continue` is not supported
NL2220  # Names starting with `nish_` are reserved for the runtime
NL2225  # Only direct calls to named functions are supported
NL2226  # Only functions can be exported for now, plus classes and interfaces (found `export` on
NL2233  # Optional/default parameters are not supported
NL2235  # Rest parameters are not supported
NL2236  # Return type mismatch: function returns
NL2252  # Unsupported `new
NL2254  # Unsupported binary operator `
NL2255  # Unsupported class member in `
NL2256  # Unsupported expression in Phase 1:
NL2259  # Unsupported operator `
NL2260  # Unsupported statement in Phase 1:
NL2261  # Unsupported unary operator `
NL2262  # Unsupported unary operator `-` on
NL2263  # Unsupported unary operator `!` on

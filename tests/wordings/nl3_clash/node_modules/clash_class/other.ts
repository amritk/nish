// The module `index.ts` imports, which declares the name `index.ts` declares again.
class Base {
  x: i32 = 0;

  constructor(x: i32) {
    this.x = x + 1;
  }
}

export const anchor = (): i32 => new Base(1).x;
